{# src/AppBundle/Resources/views/Email/feedback.html.twig #}
<p>{{ feedback.name }} {{ ( feedback.email ) ? "(" ~ feedback.email ~ ")" : null }}</p>

<p>{{ feedback.message }}</p>

<p>@{{ "now"|date("H:i, d-m-Y") }}</p>